describe("logger/bom/chrome DOM", function () {
    it("Attr", function () {
        // expect(logger(new Attr())).toEqual("[Attr]", "Attr");
    });
    it("CDATASection", function () {
        // function CDATASection() { [native code] }
        // expect(logger(new CDATASection())).toEqual("[CDATASection]", "CDATASection");
    });
    it("Comment", function () {
        // function Comment() { [native code] }
        // expect(logger(new Comment())).toEqual("[Comment]", "Comment");
    });
    it("DOMException", function () {
        // function DOMException() { [native code] }
        // expect(logger(new DOMException())).toEqual("[DOMException]", "DOMException");
    });
    it("DOMImplementation", function () {
        // function DOMImplementation() { [native code] }
        // expect(logger(new DOMImplementation())).toEqual("[DOMImplementation]", "DOMImplementation");
    });
    it("DOMParser", function () {
        // function DOMParser() { [native code] }
        // expect(logger(new DOMParser())).toEqual("[DOMParser]", "DOMParser");
    });
    it("DOMSettableTokenList", function () {
        // function DOMSettableTokenList() { [native code] }
        // expect(logger(new DOMSettableTokenList())).toEqual("[DOMSettableTokenList]", "DOMSettableTokenList");
    });
    it("DOMStringList", function () {
        // function DOMStringList() { [native code] }
        // expect(logger(new DOMStringList())).toEqual("[DOMStringList]", "DOMStringList");
    });
    it("DOMStringMap", function () {
        // function DOMStringMap() { [native code] }
        // expect(logger(new DOMStringMap())).toEqual("[DOMStringMap]", "DOMStringMap");
    });
    it("DOMTokenList", function () {
        // function DOMTokenList() { [native code] }
        // expect(logger(new DOMTokenList())).toEqual("[DOMTokenList]", "DOMTokenList");
    });
    it("Document", function () {
        // function Document() { [native code] }
        // expect(logger(new Document())).toEqual("[Document]", "Document");
    });
    it("DocumentFragment", function () {
        // function DocumentFragment() { [native code] }
        // expect(logger(new DocumentFragment())).toEqual("[DocumentFragment]", "DocumentFragment");
    });
    it("DocumentType", function () {
        // function DocumentType() { [native code] }
        // expect(logger(new DocumentType())).toEqual("[DocumentType]", "DocumentType");
    });
    it("Element", function () {
        // function Element() { [native code] }
        // expect(logger(new Element())).toEqual("[Element]", "Element");
    });
    it("Entity", function () {
        // function Entity() { [native code] }
        // expect(logger(new Entity())).toEqual("[Entity]", "Entity");
    });
    it("EntityReference", function () {
        // function EntityReference() { [native code] }
        // expect(logger(new EntityReference())).toEqual("[EntityReference]", "EntityReference");
    });
    it("HTMLAllCollection", function () {
        // function HTMLAllCollection() { [native code] }
        // expect(logger(new HTMLAllCollection())).toEqual("[HTMLAllCollection]", "HTMLAllCollection");
    });
    it("HTMLAnchorElement", function () {
        // function HTMLAnchorElement() { [native code] }
        // expect(logger(new HTMLAnchorElement())).toEqual("[HTMLAnchorElement]", "HTMLAnchorElement");
    });
    it("HTMLAppletElement", function () {
        // function HTMLAppletElement() { [native code] }
        // expect(logger(new HTMLAppletElement())).toEqual("[HTMLAppletElement]", "HTMLAppletElement");
    });
    it("HTMLAreaElement", function () {
        // function HTMLAreaElement() { [native code] }
        // expect(logger(new HTMLAreaElement())).toEqual("[HTMLAreaElement]", "HTMLAreaElement");
    });
    it("HTMLAudioElement", function () {
        // function HTMLAudioElement() { [native code] }
        // expect(logger(new HTMLAudioElement())).toEqual("[HTMLAudioElement]", "HTMLAudioElement");
    });
    it("HTMLBRElement", function () {
        // function HTMLBRElement() { [native code] }
        // expect(logger(new HTMLBRElement())).toEqual("[HTMLBRElement]", "HTMLBRElement");
    });
    it("HTMLBaseElement", function () {
        // function HTMLBaseElement() { [native code] }
        // expect(logger(new HTMLBaseElement())).toEqual("[HTMLBaseElement]", "HTMLBaseElement");
    });
    it("HTMLBaseFontElement", function () {
        // function HTMLBaseFontElement() { [native code] }
        // expect(logger(new HTMLBaseFontElement())).toEqual("[HTMLBaseFontElement]", "HTMLBaseFontElement");
    });
    it("HTMLBodyElement", function () {
        // function HTMLBodyElement() { [native code] }
        // expect(logger(new HTMLBodyElement())).toEqual("[HTMLBodyElement]", "HTMLBodyElement");
    });
    it("HTMLButtonElement", function () {
        // function HTMLButtonElement() { [native code] }
        // expect(logger(new HTMLButtonElement())).toEqual("[HTMLButtonElement]", "HTMLButtonElement");
    });
    it("HTMLCanvasElement", function () {
        // function HTMLCanvasElement() { [native code] }
        // expect(logger(new HTMLCanvasElement())).toEqual("[HTMLCanvasElement]", "HTMLCanvasElement");
    });
    it("HTMLCollection", function () {
        // function HTMLCollection() { [native code] }
        // expect(logger(new HTMLCollection())).toEqual("[HTMLCollection]", "HTMLCollection");
    });
    it("HTMLDListElement", function () {
        // function HTMLDListElement() { [native code] }
        // expect(logger(new HTMLDListElement())).toEqual("[HTMLDListElement]", "HTMLDListElement");
    });
    it("HTMLDataListElement", function () {
        // function HTMLDataListElement() { [native code] }
        // expect(logger(new HTMLDataListElement())).toEqual("[HTMLDataListElement]", "HTMLDataListElement");
    });
    it("HTMLDirectoryElement", function () {
        // function HTMLDirectoryElement() { [native code] }
        // expect(logger(new HTMLDirectoryElement())).toEqual("[HTMLDirectoryElement]", "HTMLDirectoryElement");
    });
    it("HTMLDivElement", function () {
        // function HTMLDivElement() { [native code] }
        // expect(logger(new HTMLDivElement())).toEqual("[HTMLDivElement]", "HTMLDivElement");
    });
    it("HTMLDocument", function () {
        // function HTMLDocument() { [native code] }
        // expect(logger(new HTMLDocument())).toEqual("[HTMLDocument]", "HTMLDocument");
    });
    it("HTMLElement", function () {
        // function HTMLElement() { [native code] }
        // expect(logger(new HTMLElement())).toEqual("[HTMLElement]", "HTMLElement");
    });
    it("HTMLEmbedElement", function () {
        // function HTMLEmbedElement() { [native code] }
        // expect(logger(new HTMLEmbedElement())).toEqual("[HTMLEmbedElement]", "HTMLEmbedElement");
    });
    it("HTMLFieldSetElement", function () {
        // function HTMLFieldSetElement() { [native code] }
        // expect(logger(new HTMLFieldSetElement())).toEqual("[HTMLFieldSetElement]", "HTMLFieldSetElement");
    });
    it("HTMLFontElement", function () {
        // function HTMLFontElement() { [native code] }
        // expect(logger(new HTMLFontElement())).toEqual("[HTMLFontElement]", "HTMLFontElement");
    });
    it("HTMLFormElement", function () {
        // function HTMLFormElement() { [native code] }
        // expect(logger(new HTMLFormElement())).toEqual("[HTMLFormElement]", "HTMLFormElement");
    });
    it("HTMLFrameElement", function () {
        // function HTMLFrameElement() { [native code] }
        // expect(logger(new HTMLFrameElement())).toEqual("[HTMLFrameElement]", "HTMLFrameElement");
    });
    it("HTMLFrameSetElement", function () {
        // function HTMLFrameSetElement() { [native code] }
        // expect(logger(new HTMLFrameSetElement())).toEqual("[HTMLFrameSetElement]", "HTMLFrameSetElement");
    });
    it("HTMLHRElement", function () {
        // function HTMLHRElement() { [native code] }
        // expect(logger(new HTMLHRElement())).toEqual("[HTMLHRElement]", "HTMLHRElement");
    });
    it("HTMLHeadElement", function () {
        // function HTMLHeadElement() { [native code] }
        // expect(logger(new HTMLHeadElement())).toEqual("[HTMLHeadElement]", "HTMLHeadElement");
    });
    it("HTMLHeadingElement", function () {
        // function HTMLHeadingElement() { [native code] }
        // expect(logger(new HTMLHeadingElement())).toEqual("[HTMLHeadingElement]", "HTMLHeadingElement");
    });
    it("HTMLHtmlElement", function () {
        // function HTMLHtmlElement() { [native code] }
        // expect(logger(new HTMLHtmlElement())).toEqual("[HTMLHtmlElement]", "HTMLHtmlElement");
    });
    it("HTMLIFrameElement", function () {
        // function HTMLIFrameElement() { [native code] }
        // expect(logger(new HTMLIFrameElement())).toEqual("[HTMLIFrameElement]", "HTMLIFrameElement");
    });
    it("HTMLImageElement", function () {
        // function HTMLImageElement() { [native code] }
        // expect(logger(new HTMLImageElement())).toEqual("[HTMLImageElement]", "HTMLImageElement");
    });
    it("HTMLInputElement", function () {
        // function HTMLInputElement() { [native code] }
        // expect(logger(new HTMLInputElement())).toEqual("[HTMLInputElement]", "HTMLInputElement");
    });
    it("HTMLKeygenElement", function () {
        // function HTMLKeygenElement() { [native code] }
        // expect(logger(new HTMLKeygenElement())).toEqual("[HTMLKeygenElement]", "HTMLKeygenElement");
    });
    it("HTMLLIElement", function () {
        // function HTMLLIElement() { [native code] }
        // expect(logger(new HTMLLIElement())).toEqual("[HTMLLIElement]", "HTMLLIElement");
    });
    it("HTMLLabelElement", function () {
        // function HTMLLabelElement() { [native code] }
        // expect(logger(new HTMLLabelElement())).toEqual("[HTMLLabelElement]", "HTMLLabelElement");
    });
    it("HTMLLegendElement", function () {
        // function HTMLLegendElement() { [native code] }
        // expect(logger(new HTMLLegendElement())).toEqual("[HTMLLegendElement]", "HTMLLegendElement");
    });
    it("HTMLLinkElement", function () {
        // function HTMLLinkElement() { [native code] }
        // expect(logger(new HTMLLinkElement())).toEqual("[HTMLLinkElement]", "HTMLLinkElement");
    });
    it("HTMLMapElement", function () {
        // function HTMLMapElement() { [native code] }
        // expect(logger(new HTMLMapElement())).toEqual("[HTMLMapElement]", "HTMLMapElement");
    });
    it("HTMLMarqueeElement", function () {
        // function HTMLMarqueeElement() { [native code] }
        // expect(logger(new HTMLMarqueeElement())).toEqual("[HTMLMarqueeElement]", "HTMLMarqueeElement");
    });
    it("HTMLMediaElement", function () {
        // function HTMLMediaElement() { [native code] }
        // expect(logger(new HTMLMediaElement())).toEqual("[HTMLMediaElement]", "HTMLMediaElement");
    });
    it("HTMLMenuElement", function () {
        // function HTMLMenuElement() { [native code] }
        // expect(logger(new HTMLMenuElement())).toEqual("[HTMLMenuElement]", "HTMLMenuElement");
    });
    it("HTMLMetaElement", function () {
        // function HTMLMetaElement() { [native code] }
        // expect(logger(new HTMLMetaElement())).toEqual("[HTMLMetaElement]", "HTMLMetaElement");
    });
    it("HTMLMeterElement", function () {
        // function HTMLMeterElement() { [native code] }
        // expect(logger(new HTMLMeterElement())).toEqual("[HTMLMeterElement]", "HTMLMeterElement");
    });
    it("HTMLModElement", function () {
        // function HTMLModElement() { [native code] }
        // expect(logger(new HTMLModElement())).toEqual("[HTMLModElement]", "HTMLModElement");
    });
    it("HTMLOListElement", function () {
        // function HTMLOListElement() { [native code] }
        // expect(logger(new HTMLOListElement())).toEqual("[HTMLOListElement]", "HTMLOListElement");
    });
    it("HTMLObjectElement", function () {
        // function HTMLObjectElement() { [native code] }
        // expect(logger(new HTMLObjectElement())).toEqual("[HTMLObjectElement]", "HTMLObjectElement");
    });
    it("HTMLOptGroupElement", function () {
        // function HTMLOptGroupElement() { [native code] }
        // expect(logger(new HTMLOptGroupElement())).toEqual("[HTMLOptGroupElement]", "HTMLOptGroupElement");
    });
    it("HTMLOptionElement", function () {
        // function HTMLOptionElement() { [native code] }
        // expect(logger(new HTMLOptionElement())).toEqual("[HTMLOptionElement]", "HTMLOptionElement");
    });
    it("HTMLOutputElement", function () {
        // function HTMLOutputElement() { [native code] }
        // expect(logger(new HTMLOutputElement())).toEqual("[HTMLOutputElement]", "HTMLOutputElement");
    });
    it("HTMLParagraphElement", function () {
        // function HTMLParagraphElement() { [native code] }
        // expect(logger(new HTMLParagraphElement())).toEqual("[HTMLParagraphElement]", "HTMLParagraphElement");
    });
    it("HTMLParamElement", function () {
        // function HTMLParamElement() { [native code] }
        // expect(logger(new HTMLParamElement())).toEqual("[HTMLParamElement]", "HTMLParamElement");
    });
    it("HTMLPreElement", function () {
        // function HTMLPreElement() { [native code] }
        // expect(logger(new HTMLPreElement())).toEqual("[HTMLPreElement]", "HTMLPreElement");
    });
    it("HTMLProgressElement", function () {
        // function HTMLProgressElement() { [native code] }
        // expect(logger(new HTMLProgressElement())).toEqual("[HTMLProgressElement]", "HTMLProgressElement");
    });
    it("HTMLQuoteElement", function () {
        // function HTMLQuoteElement() { [native code] }
        // expect(logger(new HTMLQuoteElement())).toEqual("[HTMLQuoteElement]", "HTMLQuoteElement");
    });
    it("HTMLScriptElement", function () {
        // function HTMLScriptElement() { [native code] }
        // expect(logger(new HTMLScriptElement())).toEqual("[HTMLScriptElement]", "HTMLScriptElement");
    });
    it("HTMLSelectElement", function () {
        // function HTMLSelectElement() { [native code] }
        // expect(logger(new HTMLSelectElement())).toEqual("[HTMLSelectElement]", "HTMLSelectElement");
    });
    it("HTMLSourceElement", function () {
        // function HTMLSourceElement() { [native code] }
        // expect(logger(new HTMLSourceElement())).toEqual("[HTMLSourceElement]", "HTMLSourceElement");
    });
    it("HTMLSpanElement", function () {
        // function HTMLSpanElement() { [native code] }
        // expect(logger(new HTMLSpanElement())).toEqual("[HTMLSpanElement]", "HTMLSpanElement");
    });
    it("HTMLStyleElement", function () {
        // function HTMLStyleElement() { [native code] }
        // expect(logger(new HTMLStyleElement())).toEqual("[HTMLStyleElement]", "HTMLStyleElement");
    });
    it("HTMLTableCaptionElement", function () {
        // function HTMLTableCaptionElement() { [native code] }
        // expect(logger(new HTMLTableCaptionElement())).toEqual("[HTMLTableCaptionElement]", "HTMLTableCaptionElement");
    });
    it("HTMLTableCellElement", function () {
        // function HTMLTableCellElement() { [native code] }
        // expect(logger(new HTMLTableCellElement())).toEqual("[HTMLTableCellElement]", "HTMLTableCellElement");
    });
    it("HTMLTableColElement", function () {
        // function HTMLTableColElement() { [native code] }
        // expect(logger(new HTMLTableColElement())).toEqual("[HTMLTableColElement]", "HTMLTableColElement");
    });
    it("HTMLTableElement", function () {
        // function HTMLTableElement() { [native code] }
        // expect(logger(new HTMLTableElement())).toEqual("[HTMLTableElement]", "HTMLTableElement");
    });
    it("HTMLTableRowElement", function () {
        // function HTMLTableRowElement() { [native code] }
        // expect(logger(new HTMLTableRowElement())).toEqual("[HTMLTableRowElement]", "HTMLTableRowElement");
    });
    it("HTMLTableSectionElement", function () {
        // function HTMLTableSectionElement() { [native code] }
        // expect(logger(new HTMLTableSectionElement())).toEqual("[HTMLTableSectionElement]", "HTMLTableSectionElement");
    });
    it("HTMLTextAreaElement", function () {
        // function HTMLTextAreaElement() { [native code] }
        // expect(logger(new HTMLTextAreaElement())).toEqual("[HTMLTextAreaElement]", "HTMLTextAreaElement");
    });
    it("HTMLTitleElement", function () {
        // function HTMLTitleElement() { [native code] }
        // expect(logger(new HTMLTitleElement())).toEqual("[HTMLTitleElement]", "HTMLTitleElement");
    });
    it("HTMLUListElement", function () {
        // function HTMLUListElement() { [native code] }
        // expect(logger(new HTMLUListElement())).toEqual("[HTMLUListElement]", "HTMLUListElement");
    });
    it("HTMLUnknownElement", function () {
        // function HTMLUnknownElement() { [native code] }
        // expect(logger(new HTMLUnknownElement())).toEqual("[HTMLUnknownElement]", "HTMLUnknownElement");
    });
    it("HTMLVideoElement", function () {
        // function HTMLVideoElement() { [native code] }
        // expect(logger(new HTMLVideoElement())).toEqual("[HTMLVideoElement]", "HTMLVideoElement");
    });
    it("NamedNodeMap", function () {
        // function NamedNodeMap() { [native code] }
        // expect(logger(new NamedNodeMap())).toEqual("[NamedNodeMap]", "NamedNodeMap");
    });
    it("Node", function () {
        // function Node() { [native code] }
        // expect(logger(new Node())).toEqual("[Node]", "Node");
    });
    it("NodeFilter", function () {
        // function NodeFilter() { [native code] }
        // expect(logger(new NodeFilter())).toEqual("[NodeFilter]", "NodeFilter");
    });
    it("NodeList", function () {
        // function NodeList() { [native code] }
        // expect(logger(new NodeList())).toEqual("[NodeList]", "NodeList");
    });
    it("Window", function () {
        // function Window() { [native code] }
        var loc = location;
        var url = loc.protocol + "//" + loc.hostname + loc.pathname;
        var win = 'Window: ' + url;
        expect(logger(window)).toEqual(win);
        expect(logger(parent)).toEqual(win);
        expect(logger(self)).toEqual(win);
        expect(logger(top)).toEqual(win);
        expect(logger(frames)).toEqual(win);
    });
    it("XMLDocument", function () {
        // function Document() { [native code] }
        // expect(logger(new Document())).toEqual("[Document]", "Document");
    });
    xit("XMLHttpRequest", function () {
        // function XMLHttpRequest() { [native code] }
        expect(logger(new XMLHttpRequest())).toEqual("{ type : '[XMLHttpRequest]', readyState : 'UNSENT (0)', statusText : '', httpCode : 0 }");
    });
    it("XMLHttpRequestException", function () {
        // function XMLHttpRequestException() { [native code] }
        // expect(logger(new XMLHttpRequestException())).toEqual("[XMLHttpRequestException]", "XMLHttpRequestException");
    });
    it("XMLHttpRequestProgressEvent", function () {
        // function XMLHttpRequestProgressEvent() { [native code] }
        // expect(logger(new XMLHttpRequestProgressEvent())).toEqual("[XMLHttpRequestProgressEvent]", "XMLHttpRequestProgressEvent");
    });
    it("XMLHttpRequestUpload", function () {
        // function XMLHttpRequestUpload() { [native code] }
        // expect(logger(new XMLHttpRequestUpload())).toEqual("[XMLHttpRequestUpload]", "XMLHttpRequestUpload");
    });
    it("XMLSerializer", function () {
        // function XMLSerializer() { [native code] }
        // expect(logger(new XMLSerializer())).toEqual("[XMLSerializer]", "XMLSerializer");
    });
    it("XPathEvaluator", function () {
        // function XPathEvaluator() { [native code] }
        // expect(logger(new XPathEvaluator())).toEqual("[XPathEvaluator]", "XPathEvaluator");
    });
    it("XPathException", function () {
        // function XPathException() { [native code] }
        // expect(logger(new XPathException())).toEqual("[XPathException]", "XPathException");
    });
    it("XPathResult", function () {
        // function XPathResult() { [native code] }
        // expect(logger(new XPathResult())).toEqual("[XPathResult]", "XPathResult");
    });
    it("XSLTProcessor", function () {
        // function XSLTProcessor() { [native code] }
        // expect(logger(new XSLTProcessor())).toEqual("[XSLTProcessor]", "XSLTProcessor");
    });
});

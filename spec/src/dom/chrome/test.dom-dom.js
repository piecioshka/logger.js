/******************************************************************************/
/* TEST CASE: logger.bom(): Chrome DOM */
/******************************************************************************/

describe("logger/bom/chrome DOM", function () {
    it("Attr", function () {
        // expect(logger(new Attr()), "[Attr]", "Attr");
    });
    it("CDATASection", function () {
        // function CDATASection() { [native code] }
        // expect(logger(new CDATASection()), "[CDATASection]", "CDATASection");
    });
    it("Comment", function () {
        // function Comment() { [native code] }
        // expect(logger(new Comment()), "[Comment]", "Comment");
    });
    it("DOMException", function () {
        // function DOMException() { [native code] }
        // expect(logger(new DOMException()), "[DOMException]", "DOMException");
    });
    it("DOMImplementation", function () {
        // function DOMImplementation() { [native code] }
        // expect(logger(new DOMImplementation()), "[DOMImplementation]", "DOMImplementation");
    });
    it("DOMParser", function () {
        // function DOMParser() { [native code] }
        // expect(logger(new DOMParser()), "[DOMParser]", "DOMParser");
    });
    it("DOMSettableTokenList", function () {
        // function DOMSettableTokenList() { [native code] }
        // expect(logger(new DOMSettableTokenList()), "[DOMSettableTokenList]", "DOMSettableTokenList");
    });
    it("DOMStringList", function () {
        // function DOMStringList() { [native code] }
        // expect(logger(new DOMStringList()), "[DOMStringList]", "DOMStringList");
    });
    it("DOMStringMap", function () {
        // function DOMStringMap() { [native code] }
        // expect(logger(new DOMStringMap()), "[DOMStringMap]", "DOMStringMap");
    });
    it("DOMTokenList", function () {
        // function DOMTokenList() { [native code] }
        // expect(logger(new DOMTokenList()), "[DOMTokenList]", "DOMTokenList");
    });
    it("Document", function () {
        // function Document() { [native code] }
        // expect(logger(new Document()), "[Document]", "Document");
    });
    it("DocumentFragment", function () {
        // function DocumentFragment() { [native code] }
        // expect(logger(new DocumentFragment()), "[DocumentFragment]", "DocumentFragment");
    });
    it("DocumentType", function () {
        // function DocumentType() { [native code] }
        // expect(logger(new DocumentType()), "[DocumentType]", "DocumentType");
    });
    it("Element", function () {
        // function Element() { [native code] }
        // expect(logger(new Element()), "[Element]", "Element");
    });
    it("Entity", function () {
        // function Entity() { [native code] }
        // expect(logger(new Entity()), "[Entity]", "Entity");
    });
    it("EntityReference", function () {
        // function EntityReference() { [native code] }
        // expect(logger(new EntityReference()), "[EntityReference]", "EntityReference");
    });
    it("HTMLAllCollection", function () {
        // function HTMLAllCollection() { [native code] }
        // expect(logger(new HTMLAllCollection()), "[HTMLAllCollection]", "HTMLAllCollection");
    });
    it("HTMLAnchorElement", function () {
        // function HTMLAnchorElement() { [native code] }
        // expect(logger(new HTMLAnchorElement()), "[HTMLAnchorElement]", "HTMLAnchorElement");
    });
    it("HTMLAppletElement", function () {
        // function HTMLAppletElement() { [native code] }
        // expect(logger(new HTMLAppletElement()), "[HTMLAppletElement]", "HTMLAppletElement");
    });
    it("HTMLAreaElement", function () {
        // function HTMLAreaElement() { [native code] }
        // expect(logger(new HTMLAreaElement()), "[HTMLAreaElement]", "HTMLAreaElement");
    });
    it("HTMLAudioElement", function () {
        // function HTMLAudioElement() { [native code] }
        // expect(logger(new HTMLAudioElement()), "[HTMLAudioElement]", "HTMLAudioElement");
    });
    it("HTMLBRElement", function () {
        // function HTMLBRElement() { [native code] }
        // expect(logger(new HTMLBRElement()), "[HTMLBRElement]", "HTMLBRElement");
    });
    it("HTMLBaseElement", function () {
        // function HTMLBaseElement() { [native code] }
        // expect(logger(new HTMLBaseElement()), "[HTMLBaseElement]", "HTMLBaseElement");
    });
    it("HTMLBaseFontElement", function () {
        // function HTMLBaseFontElement() { [native code] }
        // expect(logger(new HTMLBaseFontElement()), "[HTMLBaseFontElement]", "HTMLBaseFontElement");
    });
    it("HTMLBodyElement", function () {
        // function HTMLBodyElement() { [native code] }
        // expect(logger(new HTMLBodyElement()), "[HTMLBodyElement]", "HTMLBodyElement");
    });
    it("HTMLButtonElement", function () {
        // function HTMLButtonElement() { [native code] }
        // expect(logger(new HTMLButtonElement()), "[HTMLButtonElement]", "HTMLButtonElement");
    });
    it("HTMLCanvasElement", function () {
        // function HTMLCanvasElement() { [native code] }
        // expect(logger(new HTMLCanvasElement()), "[HTMLCanvasElement]", "HTMLCanvasElement");
    });
    it("HTMLCollection", function () {
        // function HTMLCollection() { [native code] }
        // expect(logger(new HTMLCollection()), "[HTMLCollection]", "HTMLCollection");
    });
    it("HTMLDListElement", function () {
        // function HTMLDListElement() { [native code] }
        // expect(logger(new HTMLDListElement()), "[HTMLDListElement]", "HTMLDListElement");
    });
    it("HTMLDataListElement", function () {
        // function HTMLDataListElement() { [native code] }
        // expect(logger(new HTMLDataListElement()), "[HTMLDataListElement]", "HTMLDataListElement");
    });
    it("HTMLDirectoryElement", function () {
        // function HTMLDirectoryElement() { [native code] }
        // expect(logger(new HTMLDirectoryElement()), "[HTMLDirectoryElement]", "HTMLDirectoryElement");
    });
    it("HTMLDivElement", function () {
        // function HTMLDivElement() { [native code] }
        // expect(logger(new HTMLDivElement()), "[HTMLDivElement]", "HTMLDivElement");
    });
    it("HTMLDocument", function () {
        // function HTMLDocument() { [native code] }
        // expect(logger(new HTMLDocument()), "[HTMLDocument]", "HTMLDocument");
    });
    it("HTMLElement", function () {
        // function HTMLElement() { [native code] }
        // expect(logger(new HTMLElement()), "[HTMLElement]", "HTMLElement");
    });
    it("HTMLEmbedElement", function () {
        // function HTMLEmbedElement() { [native code] }
        // expect(logger(new HTMLEmbedElement()), "[HTMLEmbedElement]", "HTMLEmbedElement");
    });
    it("HTMLFieldSetElement", function () {
        // function HTMLFieldSetElement() { [native code] }
        // expect(logger(new HTMLFieldSetElement()), "[HTMLFieldSetElement]", "HTMLFieldSetElement");
    });
    it("HTMLFontElement", function () {
        // function HTMLFontElement() { [native code] }
        // expect(logger(new HTMLFontElement()), "[HTMLFontElement]", "HTMLFontElement");
    });
    it("HTMLFormElement", function () {
        // function HTMLFormElement() { [native code] }
        // expect(logger(new HTMLFormElement()), "[HTMLFormElement]", "HTMLFormElement");
    });
    it("HTMLFrameElement", function () {
        // function HTMLFrameElement() { [native code] }
        // expect(logger(new HTMLFrameElement()), "[HTMLFrameElement]", "HTMLFrameElement");
    });
    it("HTMLFrameSetElement", function () {
        // function HTMLFrameSetElement() { [native code] }
        // expect(logger(new HTMLFrameSetElement()), "[HTMLFrameSetElement]", "HTMLFrameSetElement");
    });
    it("HTMLHRElement", function () {
        // function HTMLHRElement() { [native code] }
        // expect(logger(new HTMLHRElement()), "[HTMLHRElement]", "HTMLHRElement");
    });
    it("HTMLHeadElement", function () {
        // function HTMLHeadElement() { [native code] }
        // expect(logger(new HTMLHeadElement()), "[HTMLHeadElement]", "HTMLHeadElement");
    });
    it("HTMLHeadingElement", function () {
        // function HTMLHeadingElement() { [native code] }
        // expect(logger(new HTMLHeadingElement()), "[HTMLHeadingElement]", "HTMLHeadingElement");
    });
    it("HTMLHtmlElement", function () {
        // function HTMLHtmlElement() { [native code] }
        // expect(logger(new HTMLHtmlElement()), "[HTMLHtmlElement]", "HTMLHtmlElement");
    });
    it("HTMLIFrameElement", function () {
        // function HTMLIFrameElement() { [native code] }
        // expect(logger(new HTMLIFrameElement()), "[HTMLIFrameElement]", "HTMLIFrameElement");
    });
    it("HTMLImageElement", function () {
        // function HTMLImageElement() { [native code] }
        // expect(logger(new HTMLImageElement()), "[HTMLImageElement]", "HTMLImageElement");
    });
    it("HTMLInputElement", function () {
        // function HTMLInputElement() { [native code] }
        // expect(logger(new HTMLInputElement()), "[HTMLInputElement]", "HTMLInputElement");
    });
    it("HTMLKeygenElement", function () {
        // function HTMLKeygenElement() { [native code] }
        // expect(logger(new HTMLKeygenElement()), "[HTMLKeygenElement]", "HTMLKeygenElement");
    });
    it("HTMLLIElement", function () {
        // function HTMLLIElement() { [native code] }
        // expect(logger(new HTMLLIElement()), "[HTMLLIElement]", "HTMLLIElement");
    });
    it("HTMLLabelElement", function () {
        // function HTMLLabelElement() { [native code] }
        // expect(logger(new HTMLLabelElement()), "[HTMLLabelElement]", "HTMLLabelElement");
    });
    it("HTMLLegendElement", function () {
        // function HTMLLegendElement() { [native code] }
        // expect(logger(new HTMLLegendElement()), "[HTMLLegendElement]", "HTMLLegendElement");
    });
    it("HTMLLinkElement", function () {
        // function HTMLLinkElement() { [native code] }
        // expect(logger(new HTMLLinkElement()), "[HTMLLinkElement]", "HTMLLinkElement");
    });
    it("HTMLMapElement", function () {
        // function HTMLMapElement() { [native code] }
        // expect(logger(new HTMLMapElement()), "[HTMLMapElement]", "HTMLMapElement");
    });
    it("HTMLMarqueeElement", function () {
        // function HTMLMarqueeElement() { [native code] }
        // expect(logger(new HTMLMarqueeElement()), "[HTMLMarqueeElement]", "HTMLMarqueeElement");
    });
    it("HTMLMediaElement", function () {
        // function HTMLMediaElement() { [native code] }
        // expect(logger(new HTMLMediaElement()), "[HTMLMediaElement]", "HTMLMediaElement");
    });
    it("HTMLMenuElement", function () {
        // function HTMLMenuElement() { [native code] }
        // expect(logger(new HTMLMenuElement()), "[HTMLMenuElement]", "HTMLMenuElement");
    });
    it("HTMLMetaElement", function () {
        // function HTMLMetaElement() { [native code] }
        // expect(logger(new HTMLMetaElement()), "[HTMLMetaElement]", "HTMLMetaElement");
    });
    it("HTMLMeterElement", function () {
        // function HTMLMeterElement() { [native code] }
        // expect(logger(new HTMLMeterElement()), "[HTMLMeterElement]", "HTMLMeterElement");
    });
    it("HTMLModElement", function () {
        // function HTMLModElement() { [native code] }
        // expect(logger(new HTMLModElement()), "[HTMLModElement]", "HTMLModElement");
    });
    it("HTMLOListElement", function () {
        // function HTMLOListElement() { [native code] }
        // expect(logger(new HTMLOListElement()), "[HTMLOListElement]", "HTMLOListElement");
    });
    it("HTMLObjectElement", function () {
        // function HTMLObjectElement() { [native code] }
        // expect(logger(new HTMLObjectElement()), "[HTMLObjectElement]", "HTMLObjectElement");
    });
    it("HTMLOptGroupElement", function () {
        // function HTMLOptGroupElement() { [native code] }
        // expect(logger(new HTMLOptGroupElement()), "[HTMLOptGroupElement]", "HTMLOptGroupElement");
    });
    it("HTMLOptionElement", function () {
        // function HTMLOptionElement() { [native code] }
        // expect(logger(new HTMLOptionElement()), "[HTMLOptionElement]", "HTMLOptionElement");
    });
    it("HTMLOutputElement", function () {
        // function HTMLOutputElement() { [native code] }
        // expect(logger(new HTMLOutputElement()), "[HTMLOutputElement]", "HTMLOutputElement");
    });
    it("HTMLParagraphElement", function () {
        // function HTMLParagraphElement() { [native code] }
        // expect(logger(new HTMLParagraphElement()), "[HTMLParagraphElement]", "HTMLParagraphElement");
    });
    it("HTMLParamElement", function () {
        // function HTMLParamElement() { [native code] }
        // expect(logger(new HTMLParamElement()), "[HTMLParamElement]", "HTMLParamElement");
    });
    it("HTMLPreElement", function () {
        // function HTMLPreElement() { [native code] }
        // expect(logger(new HTMLPreElement()), "[HTMLPreElement]", "HTMLPreElement");
    });
    it("HTMLProgressElement", function () {
        // function HTMLProgressElement() { [native code] }
        // expect(logger(new HTMLProgressElement()), "[HTMLProgressElement]", "HTMLProgressElement");
    });
    it("HTMLQuoteElement", function () {
        // function HTMLQuoteElement() { [native code] }
        // expect(logger(new HTMLQuoteElement()), "[HTMLQuoteElement]", "HTMLQuoteElement");
    });
    it("HTMLScriptElement", function () {
        // function HTMLScriptElement() { [native code] }
        // expect(logger(new HTMLScriptElement()), "[HTMLScriptElement]", "HTMLScriptElement");
    });
    it("HTMLSelectElement", function () {
        // function HTMLSelectElement() { [native code] }
        // expect(logger(new HTMLSelectElement()), "[HTMLSelectElement]", "HTMLSelectElement");
    });
    it("HTMLSourceElement", function () {
        // function HTMLSourceElement() { [native code] }
        // expect(logger(new HTMLSourceElement()), "[HTMLSourceElement]", "HTMLSourceElement");
    });
    it("HTMLSpanElement", function () {
        // function HTMLSpanElement() { [native code] }
        // expect(logger(new HTMLSpanElement()), "[HTMLSpanElement]", "HTMLSpanElement");
    });
    it("HTMLStyleElement", function () {
        // function HTMLStyleElement() { [native code] }
        // expect(logger(new HTMLStyleElement()), "[HTMLStyleElement]", "HTMLStyleElement");
    });
    it("HTMLTableCaptionElement", function () {
        // function HTMLTableCaptionElement() { [native code] }
        // expect(logger(new HTMLTableCaptionElement()), "[HTMLTableCaptionElement]", "HTMLTableCaptionElement");
    });
    it("HTMLTableCellElement", function () {
        // function HTMLTableCellElement() { [native code] }
        // expect(logger(new HTMLTableCellElement()), "[HTMLTableCellElement]", "HTMLTableCellElement");
    });
    it("HTMLTableColElement", function () {
        // function HTMLTableColElement() { [native code] }
        // expect(logger(new HTMLTableColElement()), "[HTMLTableColElement]", "HTMLTableColElement");
    });
    it("HTMLTableElement", function () {
        // function HTMLTableElement() { [native code] }
        // expect(logger(new HTMLTableElement()), "[HTMLTableElement]", "HTMLTableElement");
    });
    it("HTMLTableRowElement", function () {
        // function HTMLTableRowElement() { [native code] }
        // expect(logger(new HTMLTableRowElement()), "[HTMLTableRowElement]", "HTMLTableRowElement");
    });
    it("HTMLTableSectionElement", function () {
        // function HTMLTableSectionElement() { [native code] }
        // expect(logger(new HTMLTableSectionElement()), "[HTMLTableSectionElement]", "HTMLTableSectionElement");
    });
    it("HTMLTextAreaElement", function () {
        // function HTMLTextAreaElement() { [native code] }
        // expect(logger(new HTMLTextAreaElement()), "[HTMLTextAreaElement]", "HTMLTextAreaElement");
    });
    it("HTMLTitleElement", function () {
        // function HTMLTitleElement() { [native code] }
        // expect(logger(new HTMLTitleElement()), "[HTMLTitleElement]", "HTMLTitleElement");
    });
    it("HTMLUListElement", function () {
        // function HTMLUListElement() { [native code] }
        // expect(logger(new HTMLUListElement()), "[HTMLUListElement]", "HTMLUListElement");
    });
    it("HTMLUnknownElement", function () {
        // function HTMLUnknownElement() { [native code] }
        // expect(logger(new HTMLUnknownElement()), "[HTMLUnknownElement]", "HTMLUnknownElement");
    });
    it("HTMLVideoElement", function () {
        // function HTMLVideoElement() { [native code] }
        // expect(logger(new HTMLVideoElement()), "[HTMLVideoElement]", "HTMLVideoElement");
    });
    it("NamedNodeMap", function () {
        // function NamedNodeMap() { [native code] }
        // expect(logger(new NamedNodeMap()), "[NamedNodeMap]", "NamedNodeMap");
    });
    it("Node", function () {
        // function Node() { [native code] }
        // expect(logger(new Node()), "[Node]", "Node");
    });
    it("NodeFilter", function () {
        // function NodeFilter() { [native code] }
        // expect(logger(new NodeFilter()), "[NodeFilter]", "NodeFilter");
    });
    it("NodeList", function () {
        // function NodeList() { [native code] }
        // expect(logger(new NodeList()), "[NodeList]", "NodeList");
    });
    it("Window", function () {
        // function Window() { [native code] }
        expect(logger(window)).toMatch(/Window/);
        expect(logger(parent)).toMatch(/Window/);
        expect(logger(self)).toMatch(/Window/);
        expect(logger(top)).toMatch(/Window/);
        expect(logger(frames)).toMatch(/Window/);
    });
    it("XMLDocument", function () {
        // function Document() { [native code] }
        // expect(logger(new Document()), "[Document]", "Document");
    });
    it("XMLHttpRequest", function () {
        // function XMLHttpRequest() { [native code] }
        expect(logger(new XMLHttpRequest())).toEqual({ type : '[XMLHttpRequest]', readyState : 'UNSENT (0)', statusText : '', httpCode : 0 });
    });
    it("XMLHttpRequestException", function () {
        // function XMLHttpRequestException() { [native code] }
        // expect(logger(new XMLHttpRequestException()), "[XMLHttpRequestException]", "XMLHttpRequestException");
    });
    it("XMLHttpRequestProgressEvent", function () {
        // function XMLHttpRequestProgressEvent() { [native code] }
        // expect(logger(new XMLHttpRequestProgressEvent()), "[XMLHttpRequestProgressEvent]", "XMLHttpRequestProgressEvent");
    });
    it("XMLHttpRequestUpload", function () {
        // function XMLHttpRequestUpload() { [native code] }
        // expect(logger(new XMLHttpRequestUpload()), "[XMLHttpRequestUpload]", "XMLHttpRequestUpload");
    });
    it("XMLSerializer", function () {
        // function XMLSerializer() { [native code] }
        // expect(logger(new XMLSerializer()), "[XMLSerializer]", "XMLSerializer");
    });
    it("XPathEvaluator", function () {
        // function XPathEvaluator() { [native code] }
        // expect(logger(new XPathEvaluator()), "[XPathEvaluator]", "XPathEvaluator");
    });
    it("XPathException", function () {
        // function XPathException() { [native code] }
        // expect(logger(new XPathException()), "[XPathException]", "XPathException");
    });
    it("XPathResult", function () {
        // function XPathResult() { [native code] }
        // expect(logger(new XPathResult()), "[XPathResult]", "XPathResult");
    });
    it("XSLTProcessor", function () {
        // function XSLTProcessor() { [native code] }
        // expect(logger(new XSLTProcessor()), "[XSLTProcessor]", "XSLTProcessor");
    });
});

describe("logger/bom Chrome", function () {
    it("Audio", function () {
        // [object Function]
        // expect(logger(new Audio())).toEqual("[Audio]", "Audio");
        /// TODO: Audio
        expect(1).toBe(1);
    });
    it("AudioProcessingEvent", function () {
        // function AudioProcessingEvent() { [native code] }
        // expect(logger(new AudioProcessingEvent())).toEqual("[AudioProcessingEvent]", "xxx");
        // TODO: AudioProcessingEvent
        expect(1).toBe(1);
    });
    it("BarInfo", function () {
        // BarInfo
        expect(logger(locationbar)).toEqual("[BarProp]");
        expect(logger(menubar)).toEqual("[BarProp]");
        expect(logger(scrollbars)).toEqual("[BarProp]");
        expect(logger(statusbar)).toEqual("[BarProp]");
        expect(logger(personalbar)).toEqual("[BarProp]");
        expect(logger(toolbar)).toEqual("[BarProp]");
    });
    it("BeforeLoadEvent", function () {
        // function BeforeLoadEvent() { [native code] }
        // expect(logger(new BeforeLoadEvent())).toEqual("[BeforeLoadEvent]", "xxx");
        // TODO: BeforeLoadEvent
        expect(1).toBe(1);
    });
    it("Blob", function () {
        // function Blob() { [native code] }
        // expect(logger(new Blob())).toEqual("[Blob]", "xxx");
        // TODO: Blob
        expect(1).toBe(1);
    });
    it("CanvasGradient", function () {
        // function CanvasGradient() { [native code] }
        // expect(logger(new CanvasGradient())).toEqual("[CanvasGradient]", "CanvasGradient");
        // TODO: CanvasGradient
        expect(1).toBe(1);
    });
    it("CanvasPattern", function () {
        // function CanvasPattern() { [native code] }
        // expect(logger(new CanvasPattern())).toEqual("[CanvasPattern]", "CanvasPattern");
        // TODO: CanvasPattern
        expect(1).toBe(1);
    });
    it("CanvasRenderingContext2D", function () {
        // function CanvasRenderingContext2D() { [native code] }
        // expect(logger(new CanvasRenderingContext2D())).toEqual("[CanvasRenderingContext2D]", "CanvasRenderingContext2D");
        // TODO: CanvasRenderingContext2D
        expect(1).toBe(1);
    });
    it("CharacterData", function () {
        // function CharacterData() { [native code] }
        // expect(logger(new CharacterData())).toEqual("[CharacterData]", "CharacterData");
        // TODO: CharacterData
        expect(1).toBe(1);
    });
    it("ClientRect", function () {
        // function ClientRect() { [native code] }
        // expect(logger(new ClientRect())).toEqual("[ClientRect]", "ClientRect");
        // TODO: ClientRect
        expect(1).toBe(1);
    });
    it("ClientRectList", function () {
        // function ClientRectList() { [native code] }
        // expect(logger(new ClientRectList())).toEqual("[ClientRectList]", "ClientRectList");
        // TODO: ClientRectList
        expect(1).toBe(1);
    });
    it("Clipboard", function () {
        // function Clipboard() { [native code] }
        // expect(logger(new Clipboard())).toEqual("[Clipboard]", "Clipboard");
        // TODO: Clipboard
        expect(1).toBe(1);
    });
    it("CloseEvent", function () {
        // function CloseEvent() { [native code] }
        // expect(logger(new CloseEvent())).toEqual("[CloseEvent]", "CloseEvent");
        // TODO: CloseEvent
        expect(1).toBe(1);
    });
    it("CompositionEvent", function () {
        // function CompositionEvent() { [native code] }
        // expect(logger(new CompositionEvent())).toEqual("[CompositionEvent]", "CompositionEvent");
        // TODO: CompositionEvent
        expect(1).toBe(1);
    });
    it("Counter", function () {
        // function Counter() { [native code] }
        // expect(logger(new Counter())).toEqual("[Counter]", "Counter");
        // TODO: Counter
        expect(1).toBe(1);
    });
    it("CustomEvent", function () {
        // function CustomEvent() { [native code] }
        // expect(logger(new CustomEvent())).toEqual("[CustomEvent]", "CustomEvent");
        // TODO: CustomEvent
        expect(1).toBe(1);
    });
    it("DataView", function () {
        // code in dir "dom"
        // TODO: DataView
        expect(1).toBe(1);
    });
    it("DeviceOrientationEvent", function () {
        // function DeviceOrientationEvent() { [native code] }
        // expect(logger(new DeviceOrientationEvent())).toEqual("[DeviceOrientationEvent]", "DeviceOrientationEvent");
        // TODO: DeviceOrientationEvent
        expect(1).toBe(1);
    });
    it("ErrorEvent", function () {
        // function ErrorEvent() { [native code] }
        // expect(logger(new ErrorEvent())).toEqual("[ErrorEvent]", "ErrorEvent");
        // TODO: ErrorEvent
        expect(1).toBe(1);
    });
    it("Event", function () {
        // function Event() { [native code] }
        // expect(logger(new Event())).toEqual("[Event]", "Event");
        // TODO: Event
        expect(1).toBe(1);
    });
    it("EventException", function () {
        // function EventException() { [native code] }
        // expect(logger(new EventException())).toEqual("[EventException]", "EventException");
        // TODO: EventException
        expect(1).toBe(1);
    });
    it("EventSource", function () {
        // function EventSource() { [native code] }
        // expect(logger(new EventSource())).toEqual("[EventSource]", "EventSource");
        // TODO: EventSource
        expect(1).toBe(1);
    });
    it("File", function () {
        // function File() { [native code] }
        // expect(logger(new File())).toEqual("[File]", "File");
        // TODO: File
        expect(1).toBe(1);
    });
    it("FileError", function () {
        // function FileError() { [native code] }
        // expect(logger(new FileError())).toEqual("[FileError]", "FileError");
        // TODO: FileError
        expect(1).toBe(1);
    });
    it("FileList", function () {
        // function FileList() { [native code] }
        // expect(logger(new FileList())).toEqual("[FileList]", "FileList");
        // TODO: FileList
        expect(1).toBe(1);
    });
    it("FileReader", function () {
        // function FileReader() { [native code] }
        // expect(logger(new FileReader())).toEqual("[FileReader]", "FileReader");
        // TODO: FileReader
        expect(1).toBe(1);
    });
    it("FormData", function () {
        // function FormData() { [native code] }
        // expect(logger(new FormData())).toEqual("[FormData]", "FormData");
        // TODO: FormData
        expect(1).toBe(1);
    });
    it("HashChangeEvent", function () {
        // function HashChangeEvent() { [native code] }
        // expect(logger(new HashChangeEvent())).toEqual("[HashChangeEvent]", "HashChangeEvent");
        // TODO: HashChangeEvent
        expect(1).toBe(1);
    });
    it("IceCandidate", function () {
        // function IceCandidate() { [native code] }
        // expect(logger(new IceCandidate())).toEqual("[IceCandidate]", "IceCandidate");
        // TODO: IceCandidate
        expect(1).toBe(1);
    });
    it("Image", function () {
        // [object Function]
        // expect(logger(new Image())).toEqual("[Image]", "Image");
        // TODO: Image
        expect(1).toBe(1);
    });
    it("ImageData", function () {
        // function ImageData() { [native code] }
        // expect(logger(new ImageData())).toEqual("[ImageData]", "ImageData");
        // TODO: ImageData
        expect(1).toBe(1);
    });
    it("KeyboardEvent", function () {
        // function KeyboardEvent() { [native code] }
        // expect(logger(new KeyboardEvent())).toEqual("[KeyboardEvent]", "KeyboardEvent");
        // TODO: KeyboardEvent
        expect(1).toBe(1);
    });
    it("MediaController", function () {
        // function MediaController() { [native code] }
        // expect(logger(new MediaController())).toEqual("[MediaController]", "MediaController");
        // TODO: MediaController
        expect(1).toBe(1);
    });
    it("MediaError", function () {
        // function MediaError() { [native code] }
        // expect(logger(new MediaError())).toEqual("[MediaError]", "MediaError");
        // TODO: MediaError
        expect(1).toBe(1);
    });
    it("MediaList", function () {
        // function MediaList() { [native code] }
        // expect(logger(new MediaList())).toEqual("[MediaList]", "MediaList");
        // TODO: MediaList
        expect(1).toBe(1);
    });
    it("MediaStreamEvent", function () {
        // function MediaStreamEvent() { [native code] }
        // expect(logger(new MediaStreamEvent())).toEqual("[MediaStreamEvent]", "MediaStreamEvent");
        // TODO: MediaStreamEvent
        expect(1).toBe(1);
    });
    it("MessageChannel", function () {
        // function MessageChannel() { [native code] }
        // expect(logger(new MessageChannel())).toEqual("[MessageChannel]", "MessageChannel");
        // TODO: MessageChannel
        expect(1).toBe(1);
    });
    it("MessageEvent", function () {
        // function MessageEvent() { [native code] }
        // expect(logger(new MessageEvent())).toEqual("[MessageEvent]", "MessageEvent");
        // TODO: MessageEvent
        expect(1).toBe(1);
    });
    it("MessagePort", function () {
        // function MessagePort() { [native code] }
        // expect(logger(new MessagePort())).toEqual("[MessagePort]", "MessagePort");
        // TODO: MessagePort
        expect(1).toBe(1);
    });
    it("MimeType", function () {
        // function MimeType() { [native code] }
        // expect(logger(new MimeType())).toEqual("[MimeType]", "MimeType");
        // TODO: MimeType
        expect(1).toBe(1);
    });
    it("MimeTypeArray", function () {
        // function MimeTypeArray() { [native code] }
        // expect(logger(new MimeTypeArray())).toEqual("[MimeTypeArray]", "MimeTypeArray");
        // TODO: MimeTypeArray
        expect(1).toBe(1);
    });
    it("MouseEvent", function () {
        // function MouseEvent() { [native code] }
        // expect(logger(new MouseEvent())).toEqual("[MouseEvent]", "MouseEvent");
        // TODO: MouseEvent
        expect(1).toBe(1);
    });
    it("MutationEvent", function () {
        // function MutationEvent() { [native code] }
        // expect(logger(new MutationEvent())).toEqual("[MutationEvent]", "MutationEvent");
        // TODO: MutationEvent
        expect(1).toBe(1);
    });
    it("Notation", function () {
        // function Notation() { [native code] }
        // expect(logger(new Notation())).toEqual("[Notation]", "Notation");
        // TODO: Notation
        expect(1).toBe(1);
    });
    it("Notification", function () {
        // function Notification() { [native code] }
        // expect(logger(new Notification())).toEqual("[Notification]", "Notification");
        // TODO: Notification
        expect(1).toBe(1);
    });
    it("OfflineAudioCompletionEvent", function () {
        // function OfflineAudioCompletionEvent() { [native code] }
        // expect(logger(new OfflineAudioCompletionEvent())).toEqual("[OfflineAudioCompletionEvent]", "OfflineAudioCompletionEvent");
        // TODO: OfflineAudioCompletionEvent
        expect(1).toBe(1);
    });
    it("Option", function () {
        // [object Function]
        // expect(logger(new Option())).toEqual("[Option]", "Option");
        // TODO: Option
        expect(1).toBe(1);
    });
    it("OverflowEvent", function () {
        // function OverflowEvent() { [native code] }
        // expect(logger(new OverflowEvent())).toEqual("[OverflowEvent]", "OverflowEvent");
        // TODO: OverflowEvent
        expect(1).toBe(1);
    });
    it("PageTransitionEvent", function () {
        // function PageTransitionEvent() { [native code] }
        // expect(logger(new PageTransitionEvent())).toEqual("[PageTransitionEvent]", "PageTransitionEvent");
        // TODO: PageTransitionEvent
        expect(1).toBe(1);
    });
    it("Plugin", function () {
        // function Plugin() { [native code] }
        // expect(logger(new Plugin())).toEqual("[Plugin]", "Plugin");
        // TODO: Plugin
        expect(1).toBe(1);
    });
    it("PluginArray", function () {
        // function PluginArray() { [native code] }
        // expect(logger(new PluginArray())).toEqual("[PluginArray]", "PluginArray");
        // TODO: PluginArray
        expect(1).toBe(1);
    });
    it("PopStateEvent", function () {
        // function PopStateEvent() { [native code] }
        // expect(logger(new PopStateEvent())).toEqual("[PopStateEvent]", "PopStateEvent");
        // TODO: PopStateEvent
        expect(1).toBe(1);
    });
    it("ProcessingInstruction", function () {
        // function ProcessingInstruction() { [native code] }
        // expect(logger(new ProcessingInstruction())).toEqual("[ProcessingInstruction]", "ProcessingInstruction");
        // TODO: ProcessingInstruction
        expect(1).toBe(1);
    });
    it("ProgressEvent", function () {
        // function ProgressEvent() { [native code] }
        // expect(logger(new ProgressEvent())).toEqual("[ProgressEvent]", "ProgressEvent");
        // TODO: ProgressEvent
        expect(1).toBe(1);
    });
    it("RGBColor", function () {
        // function RGBColor() { [native code] }
        // expect(logger(new RGBColor())).toEqual("[RGBColor]", "RGBColor");
        // TODO: RGBColor
        expect(1).toBe(1);
    });
    it("Range", function () {
        // function Range() { [native code] }
        // expect(logger(new Range())).toEqual("[Range]", "Range");
        // TODO: Range
        expect(1).toBe(1);
    });
    it("RangeException", function () {
        // function RangeException() { [native code] }
        // expect(logger(new RangeException())).toEqual("[RangeException]", "RangeException");
        // TODO: RangeException
        expect(1).toBe(1);
    });
    it("Rect", function () {
        // function Rect() { [native code] }
        // expect(logger(new Rect())).toEqual("[Rect]", "Rect");
        // TODO: Rect
        expect(1).toBe(1);
    });
    it("SQLException", function () {
        // function SQLException() { [native code] }
        // expect(logger(new SQLException())).toEqual("[SQLException]", "SQLException");
        // TODO: SQLException
        expect(1).toBe(1);
    });
    it("Selection", function () {
        // function Selection() { [native code] }
        // expect(logger(new Selection())).toEqual("[Selection]", "Selection");
        // TODO: Selection
        expect(1).toBe(1);
    });
    it("SessionDescription", function () {
        // function SessionDescription() { [native code] }
        // expect(logger(new SessionDescription())).toEqual("[SessionDescription]", "SessionDescription");
        // TODO: SessionDescription
        expect(1).toBe(1);
    });
    it("SharedWorker", function () {
        // function SharedWorker() { [native code] }
        // expect(logger(new SharedWorker())).toEqual("[SharedWorker]", "SharedWorker");
        // TODO: SharedWorker
        expect(1).toBe(1);
    });
    it("SpeechInputEvent", function () {
        // function SpeechInputEvent() { [native code] }
        // expect(logger(new SpeechInputEvent())).toEqual("[SpeechInputEvent]", "SpeechInputEvent");
        // TODO: SpeechInputEvent
        expect(1).toBe(1);
    });
    it("Storage", function () {
        // function Storage() { [native code] }
        expect(logger(localStorage)).toEqual("[Storage]", "Storage");
        expect(logger(sessionStorage)).toEqual("[Storage]", "Storage");
    });
    it("StorageEvent", function () {
        // function StorageEvent() { [native code] }
        // expect(logger(new StorageEvent())).toEqual("[StorageEvent]", "StorageEvent");
        // TODO: StorageEvent
        expect(1).toBe(1);
    });
    it("StyleSheet", function () {
        // function StyleSheet() { [native code] }
        // expect(logger(new StyleSheet())).toEqual("[StyleSheet]", "StyleSheet");
        // TODO: StyleSheet
        expect(1).toBe(1);
    });
    it("StyleSheetList", function () {
        // function StyleSheetList() { [native code] }
        // expect(logger(new StyleSheetList())).toEqual("[StyleSheetList]", "StyleSheetList");
        // TODO: StyleSheetList
        expect(1).toBe(1);
    });
    it("Text", function () {
        // function Text() { [native code] }
        // expect(logger(new Text())).toEqual("[Text]", "Text");
        // TODO: Text
        expect(1).toBe(1);
    });
    it("TextEvent", function () {
        // function TextEvent() { [native code] }
        // expect(logger(new TextEvent())).toEqual("[TextEvent]", "TextEvent");
        // TODO: TextEvent
        expect(1).toBe(1);
    });
    it("TextMetrics", function () {
        // function TextMetrics() { [native code] }
        // expect(logger(new TextMetrics())).toEqual("[TextMetrics]", "TextMetrics");
        // TODO: TextMetrics
        expect(1).toBe(1);
    });
    it("TimeRanges", function () {
        // function TimeRanges() { [native code] }
        // expect(logger(new TimeRanges())).toEqual("[TimeRanges]", "TimeRanges");
        // TODO: TimeRanges
        expect(1).toBe(1);
    });
    it("TouchEvent", function () {
        // function TouchEvent() { [native code] }
        // expect(logger(new TouchEvent())).toEqual("[TouchEvent]", "TouchEvent");
        // TODO: TouchEvent
        expect(1).toBe(1);
    });
    it("UIEvent", function () {
        // function UIEvent() { [native code] }
        // expect(logger(new UIEvent())).toEqual("[UIEvent]", "UIEvent");
        // TODO: UIEvent
        expect(1).toBe(1);
    });
    it("WebSocket", function () {
        // function WebSocket() { [native code] }
        // expect(logger(new WebSocket())).toEqual("[WebSocket]", "WebSocket");
        // TODO: WebSocket
        expect(1).toBe(1);
    });
    it("WheelEvent", function () {
        // function WheelEvent() { [native code] }
        // expect(logger(new WheelEvent())).toEqual("[WheelEvent]", "WheelEvent");
        // TODO: WheelEvent
        expect(1).toBe(1);
    });
    it("Worker", function () {
        // function Worker() { [native code] }
        // expect(logger(new Worker())).toEqual("[Worker]", "Worker");
        // TODO: Worker
        expect(1).toBe(1);
    });
    it("clientInformation", function () {
        // Navigator
        // expect(logger(new Navigator())).toEqual("[Navigator]", "Navigator");
        // TODO: clientInformation
        expect(1).toBe(1);
    });
    it("console", function () {
        // Console
        // expect(logger(new Console())).toEqual("[Console]", "Console");
        // TODO: console
        expect(1).toBe(1);
    });
    it("crypto", function () {
        // Crypto
        // expect(logger(new Crypto())).toEqual("[Crypto]", "Crypto");
        // TODO: crypto
        expect(1).toBe(1);
    });
    it("document", function () {
        // HTMLDocument
        // expect(logger(new HTMLDocument())).toEqual("[HTMLDocument]", "HTMLDocument");
        // TODO: HTMLDocument
        expect(1).toBe(1);
    });
    it("history", function () {
        // History
        // expect(logger(new History())).toEqual("[History]", "History");
        // TODO: History
        expect(1).toBe(1);
    });
    it("location", function () {
        // Location
        // expect(logger(new Location())).toEqual("[Location]", "Location");
        // TODO: Location
        expect(1).toBe(1);
    });
    it("navigator", function () {
        // Navigator
        // expect(logger(new Navigator())).toEqual("[Navigator]", "Navigator");
        // TODO: Navigator
        expect(1).toBe(1);
    });
    it("performance", function () {
        // Performance
        // expect(logger(new Performance())).toEqual("[Performance]", "Performance");
        // TODO: Performance
        expect(1).toBe(1);
    });
    it("screen", function () {
        // Screen
        // expect(logger(new Screen())).toEqual("[Screen]", "Screen");
        // TODO: Screen
        expect(1).toBe(1);
    });
    it("styleMedia", function () {
        // StyleMedia
        // expect(logger(new StyleMedia())).toEqual("[StyleMedia]", "StyleMedia");
        // TODO: StyleMedia
        expect(1).toBe(1);
    });
    it("v8Intl", function () {
        // Object
        // expect(logger(new v8Intl())).toEqual("[v8Intl]", "v8Intl");
        // TODO: v8Intl
        expect(1).toBe(1);
    });
});

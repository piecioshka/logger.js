/******************************************************************************/
/* DOM XPath Level 3 */
/******************************************************************************/

// http://www.w3.org/TR/2004/NOTE-DOM-Level-3-XPath-20040226/xpath.html
describe("logger/dom DOM XPath Level 3", function () {
    // 1.4 Interfaces

    // exceptions
    it("[exception XPathException]", function () {
        expect(false);
    });

    // interfaces
    it("[interface XPathEvaluator]", function () {
        expect(false);
    });
    it("[interface XPathExpression]", function () {
        expect(false);
    });
    it("[interface XPathNSResolver]", function () {
        expect(false);
    });
    it("[interface XPathResult]", function () {
        expect(false);
    });
    it("[interface XPathNamespace : Node]", function () {
        expect(false);
    });
});

// http://www.w3.org/TR/2003/REC-DOM-Level-2-HTML-20030109/html.html
describe("logger/dom DOM HTML Level 2", function () {
    // 1.4. Miscellaneous Object Definitions

    // interfaces
    it("[interface HTMLCollection]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLOptionsCollection]", function () {
        expect(1).toEqual(1);
    });

    // 1.5. Objects related to HTML documents

    // interfaces
    it("[interface HTMLDocument : Document]", function () {
        expect(1).toEqual(1);
    });

    // 1.6.4. The HTMLElement interface

    // interfaces
    it("[interface HTMLElement : Element]", function () {
        expect(1).toEqual(1);
    });

    // 1.6.5. Object definitions

    // interfaces
    it("[interface HTMLHtmlElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLHeadElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLLinkElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLTitleElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLMetaElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLBaseElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLIsIndexElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLStyleElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLBodyElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLFormElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLSelectElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLOptGroupElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLOptionElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLInputElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLTextAreaElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLButtonElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLLabelElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLFieldSetElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLLegendElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLUListElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLOListElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLDListElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLDirectoryElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLMenuElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLLIElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLDivElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLParagraphElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLHeadingElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLQuoteElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLPreElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLBRElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLBaseFontElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLFontElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLHRElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLModElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLAnchorElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLImageElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLObjectElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLParamElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLAppletElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLMapElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLAreaElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLScriptElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLTableElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLTableCaptionElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLTableColElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLTableSectionElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLTableRowElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLTableCellElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLFrameSetElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLFrameElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
    it("[interface HTMLIFrameElement : HTMLElement]", function () {
        expect(1).toEqual(1);
    });
});
